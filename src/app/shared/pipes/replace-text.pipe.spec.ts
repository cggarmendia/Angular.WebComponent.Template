import { ReplaceTextPipe } from './replace-text.pipe';

describe('ReplaceTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceTextPipe();
    expect(pipe).toBeTruthy();
  });
});
